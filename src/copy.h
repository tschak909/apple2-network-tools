#ifndef COPY_H
#define COPY_H

/* Browse a FujiNet network directory and copy selected files to local prefix. */
void copy_from_net_screen(void);

/* Browse the local ProDOS prefix and copy selected files to a network prefix. */
void copy_to_net_screen(void);

#endif /* COPY_H */
