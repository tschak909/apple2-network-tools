set local prefix immediately go back to menu
local -> net show local first then network.
local -> net show file type, then blocks, then modified date as 30-DEC-23
local -> net show blocks used and blocks free

check in


